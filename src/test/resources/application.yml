spring:
  config:
    additional-location: classpath:serenity.conf

compose:
  worker-ip: 10.150.19.71
  calculatorPath: src/test/resources/compose/docker-compose.yml
  seleniumGridPath: src/test/resources/compose/selenium/docker-compose-v3-full-grid.yml

selenium:
  grid4:
    url: "http://${calculator-ui.ip}:4444/wd/hub"

application:
  url1: "http://${calculator-ui.ip}:${calculator-ui.port}"

calculator-ui:
  ip: ${compose.worker-ip}
  port: 8073
  hostname: calculator-ui
  first-value-element-id: firstValue
  second-value-element-id: secondValue
  calculation-select-element-id: calculationSelect
  calculation-button-element-id: calculationButton
  result-value-element-id: resultValue
  result-message-element-id: resultMessage
